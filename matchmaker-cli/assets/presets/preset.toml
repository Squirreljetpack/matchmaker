[start]
command = 'mm --presets'
# on_accept = 'mm -o {}'
directory = { value = 'dirname -- "$MM_OVERRIDE"', exec = true, force = true }

[binds]
"@accept" = "Become(mm -o {})"
"@Accept" = "Accept"

[columns]
max = 1
