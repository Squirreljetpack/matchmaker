might not be too hard to convert to a rust cli, if we can implement the full fzf functionality:
keybinds
preview

multi panes?

a tui should allow defining custom command, and running them on the highlighted element

how do we keep it running in the interim after fzs migration?